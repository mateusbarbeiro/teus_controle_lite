name: teus_controle_lite
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.0.0+3
  path:
  get_it:
  mobx:
  flutter_mobx:
  background_app_bar:
  url_launcher:
  cloud_firestore: ^2.5.3
  firebase_core: ^1.6.0
  http:
  dio: ^4.0.0
  flutter_spinkit: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner:
  mobx_codegen:

flutter:
  uses-material-design: true
  assets:
    - lib/app/assets/basketImage.jpg
